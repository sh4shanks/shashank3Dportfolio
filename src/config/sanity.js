// Sanity is intentionally disabled for this personal portfolio.
// Local project/content data is used so the template owner's CMS content never appears.
export const sanityClient = null;
export const urlFor = () => null;
export const getProxyUrl = (url) => url || null;
